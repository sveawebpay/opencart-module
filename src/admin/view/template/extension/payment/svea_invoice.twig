{{ header }}{{ column_left }}

{% if version > 3.0 %}
<div id="content">
    <div class="page-header">
        <div class="container-fluid">
            <div class="float-right">
                <button type="submit" form="form-module" data-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fas fa-check-circle"></i></button>
                <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-light"><i class="fas fa-reply"></i></a></div>
            <h1>{{ heading_title }}</h1>
            <ol class="breadcrumb">
                {% for breadcrumb in breadcrumbs %}
                    <li class="breadcrumb-item"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
                {% endfor %}
            </ol>
        </div>
        <div class="container-fluid">
            {% if error_warning %}
                <div class="alert alert-danger alert-dismissible"><i class="fas fa-exclamation-circle"></i> {{ error_warning }}
                    <button type="button" class="close" data-dismiss="alert">&times;</button>
                </div>
            {% endif %}
            <div class="card">
                <div class="card-header"><i class="fas fa-pencil-alt"></i> {{ text_edit }}</div>
                <div class="card-body">
        <!--general settings -->
        <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-sveainvoice" class="form-module">
                <div class="form-group row">
                    <div class="col-sm-2 col-form-label">Version</div>
                    <div class="col-sm-9">
                        <input type="text" readonly class="form-control-plaintext" value="{{ payment_svea_version_text }}">
                    </div>
                    <input class="form-control" type="hidden" value="{{ payment_svea_version }}" name="payment_svea_invoice_version" id="payment_svea_invoice_version" />
                </div>
                <div class="form-group row">
                    <label for="payment_svea_invoice_geo_zone_id" class="col-sm-2 col-form-label">{{ entry_geo_zone }}</label>
                    <div class="col-sm-9">
                        <select class="form-control" name="payment_svea_invoice_geo_zone_id" >
                            <option value="0">{{ text_all_zones }}</option>
                            {% for geo_zone in geo_zones %}
                            {% if geo_zone.geo_zone_id  ==  payment_svea_invoice_geo_zone_id %}
                            <option value="{{ geo_zone.geo_zone_id }}" selected="selected">{{ geo_zone.name }}</option>
                             {% else %}
                            <option value="{{ geo_zone.geo_zone_id }}">{{ geo_zone.name }}</option>
                            {% endif %}
                            {% endfor %}
                        </select>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="payment_svea_invoice_status" class="col-sm-2 col-form-label">{{ entry_status }}</label>
                    <div class="col-sm-9">
                        <select class="form-control" name="payment_svea_invoice_status">
                            {% if payment_svea_invoice_status %}
                            <option value="1" selected="selected">{{ text_enabled }}</option>
                            <option value="0">{{ text_disabled }}</option>
                             {% else %}
                            <option value="1">{{ text_enabled }}</option>
                            <option value="0" selected="selected">{{ text_disabled }}</option>
                            {% endif %}
                        </select>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="payment_svea_invoice_sort_order" class="col-sm-2 col-form-label">{{ entry_sort_order }}</label>
                    <div class="col-sm-9">
                       <input class="form-control" type="text" name="payment_svea_invoice_sort_order" value="{{ payment_svea_invoice_sort_order }}" size="1" />
                    </div>
                </div>
                <div class="form-group row">
                    <label for="payment_svea_invoice_payment_description" class="col-sm-2 col-form-label">{{ entry_payment_description }}</label>
                    <div class="col-sm-9">
                     <textarea class="form-control" rows="2" cols="30" name="payment_svea_invoice_payment_description">{{ payment_svea_invoice_payment_description }}</textarea>
                    </div>
                </div>
                    <!--shipping billing-->
                    <div class="form-group row">
                        <label for="payment_svea_invoice_shipping_billing" class="col-sm-2 col-form-label">
                            <span data-toggle="tooltip" title="{{ entry_shipping_billing_text }}">{{ entry_shipping_billing }}</span>
                        </label>
                         <div class="col-sm-9" style="padding-top:15px">
                           {% if payment_svea_invoice_shipping_billing  ==  "0" %}
                            <input type="radio" name="payment_svea_invoice_shipping_billing" value="1" />
                            {{ entry_yes }}
                            <input type="radio" name="payment_svea_invoice_shipping_billing" value="0" checked="checked" />
                            {{ entry_no }}
                             {% else %}
                            <input type="radio" name="payment_svea_invoice_shipping_billing" value="1" checked="checked" />
                            {{ entry_yes }}
                            <input type="radio" name="payment_svea_invoice_shipping_billing" value="0" />
                            {{ entry_no }}
                            {% endif %}
                         </div>
                    </div>
                    <!-- autodeliver -->
                    <div class="form-group row">
                        <label for="payment_svea_invoice_auto_deliver" class="col-sm-2 col-form-label">
                            <span data-toggle="tooltip" title="{{ entry_auto_deliver_text }}">{{ entry_auto_deliver }}</span>
                        </label>
                         <div class="col-sm-9">
                            <select class="form-control" name="payment_svea_invoice_auto_deliver">
                                <option value="0" {% if payment_svea_invoice_auto_deliver  ==  '0'%} {{ 'selected="selected"' }}{% endif %} >{{ text_disabled }}</option>
                                <option value="1" {% if payment_svea_invoice_auto_deliver  ==  '1'%} {{ 'selected="selected"' }}{% endif %} >{{ text_enabled }}</option>
                            </select>
                         </div>
                    </div>
                    <div class="form-group row">
                        <label for="entry_distribution_type" class="col-sm-2 col-form-label">
                            {{ entry_distribution_type }}
                        </label>
                        <div class="col-sm-9">
                            <select class="form-control" name="payment_svea_invoice_distribution_type">
                                <option value="Post" {% if payment_svea_invoice_distribution_type  ==  'Post'%} {{ 'selected="selected"' }}{% endif %} >{{ entry_post }}</option>
                                <option value="Email" {% if payment_svea_invoice_distribution_type  ==  'Email'%} {{ 'selected="selected"' }}{% endif %} >{{ entry_email }}</option>
                            </select>
                         </div>
                    </div>
                   <div class="form-group row">
                        <label for="payment_svea_invoice_product_price" class="col-sm-2 col-form-label">
                            <span data-toggle="tooltip" title="{{ entry_product_text }}">{{ entry_product }}</span>
                        </label>
                        <div class="col-sm-9" style="padding-top:15px;">
                           {% if payment_svea_invoice_product_price %}
                            <input type="radio" name="payment_svea_invoice_product_price" value="1" checked="checked" />
                            {{ entry_yes }}
                            <input type="radio" name="payment_svea_invoice_product_price" value="0" />
                            {{ entry_no }}
                             {% else %}
                            <input type="radio" name="payment_svea_invoice_product_price" value="1" />
                            {{ entry_yes }}
                            <input type="radio" name="payment_svea_invoice_product_price" value="0" checked="checked" />
                            {{ entry_no }}
                           {% endif %}
                    </div>
                </div>
            <div class="form-group row">
                <label for="payment_svea_invoice_peppol" class="col-sm-2 col-form-label">
                    {{ entry_show_peppol_field }}
                </label>
                <div class="col-sm-9" style="padding-top:15px;">
                    {% if payment_svea_invoice_peppol %}
                        <input type="radio" name="payment_svea_invoice_peppol" value="1" checked="checked" />
                        {{ entry_yes }}
                        <input type="radio" name="payment_svea_invoice_peppol" value="0" />
                        {{ entry_no }}
                    {% else %}
                        <input type="radio" name="payment_svea_invoice_peppol" value="1" />
                        {{ entry_yes }}
                        <input type="radio" name="payment_svea_invoice_peppol" value="0" checked="checked" />
                        {{ entry_no }}
                    {% endif %}
                </div>
            </div>
            <!-- Countrycode specific -->
            <div class="panel-body" id="tab-invoice" >
                <ul class="nav nav-tabs" id="payment_svea_country">
                    {% for code in credentials %}
                    <li class="nav-item"><a class="nav-link"href="#tab-{{ code.lang}}" data-toggle="tab">{{ code.lang }}</a></li>
                    {% endfor %}
                </ul>
                <div class="tab-content">
                {% for code in credentials %}
                    <div class="tab-pane" id="tab-{{ code.lang }}">
                        <div class="form-group row">
                            <label class="col-sm-2 col-form-label" for="{{ code.name_testmode }}">{{ entry_testmode }}</label>
                            <div class="col-sm-9">
                                <select class="form-control" name="{{ code.name_testmode }}">
                                    <option value="1" {% if code.value_testmode  ==  '1'%} {{ 'selected="selected"' }}{% endif %} >{{ text_enabled }}</option>
                                    <option value="0" {% if code.value_testmode  ==  '0'%} {{ 'selected="selected"' }}{% endif %} >{{ text_disabled }}</option>
                                </select>
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-sm-2 col-form-label" for="{{ code.name_username }}">{{ entry_username }}</label>
                            <div class="col-sm-9">
                                  <input class="form-control" name="{{ code.name_username }}" type="text"
                                           value="{{ code.value_username }}" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-sm-2 col-form-label" for="{{ code.name_password }}">{{ entry_password }}</label>
                            <div class="col-sm-9">
                                 <input class="form-control" name="{{ code.name_password }}" type="text"
                                           value="{{ code.value_password }}" />
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-sm-2 col-form-label" for="{{ code.name_clientno }}">{{ entry_clientno }}</label>
                            <div class="col-sm-9">
                                 <input class="form-control" name="{{ code.name_clientno }}" type="text"
                                           value="{{ code.value_clientno }}" />
                            </div>
                        </div>
                    </div>
                {% endfor %}
                </div>
            </div>
        </form>
    </div>
    </div><!-- panel-default -->
  </div><!-- container-fluid -->

    {% else %}

    <div id="content">
        <div class="page-header">
            <div class="container-fluid">
                <div class="pull-right">
                    <button type="submit" onclick="$('#form').submit();" form="form-sveainvoice" data-toggle="tooltip" title="{{ button_save }}" class="btn btn-primary"><i class="fa fa-save"></i></button>
                    <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default"><i class="fa fa-reply"></i></a></div>
                <h1>{{ heading_title }}</h1>
                <ul class="breadcrumb">
                    {% for breadcrumb in breadcrumbs %}
                        <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
                    {% endfor %}
                </ul>
            </div>
        </div>
        <div class="container-fluid">
            {% if error_warning %}
                <div class="alert alert-danger"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}
                    <button type="button" class="close" data-dismiss="alert">&times;</button>
                </div>
            {% endif %}
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title"><i class="fa fa-pencil"></i> </h3>
                </div>
                <div class="panel-body">
                    <!--general settings -->
                    <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-sveainvoice" class="form-horizontal">
                        <div class="form-group">
                            <label class="col-sm-2 control-label">Version</label>
                            <div class="col-sm-9">
                                <p class="form-control-static">{{ payment_svea_version_text }}</p>
                            </div>
                            <input class="form-control" type="hidden" value="{{ payment_svea_version }}" name="payment_svea_invoice_version" id="payment_svea_invoice_version" />
                        </div>
                        <div class="form-group">
                            <label for="payment_svea_invoice_geo_zone_id" class="col-sm-2 control-label">{{ entry_geo_zone }}</label>
                            <div class="col-sm-9">
                                <select class="form-control" name="payment_svea_invoice_geo_zone_id">
                                    <option value="0">{{ text_all_zones }}</option>
                                    {% for geo_zone in geo_zones %}
                                        {% if geo_zone.geo_zone_id  ==  payment_svea_invoice_geo_zone_id %}
                                            <option value="{{ geo_zone.geo_zone_id }}" selected="selected">{{ geo_zone.name }}</option>
                                        {% else %}
                                            <option value="{{ geo_zone.geo_zone_id }}">{{ geo_zone.name }}</option>
                                        {% endif %}
                                    {% endfor %}
                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="payment_svea_invoice_status" class="col-sm-2 control-label">{{ entry_status }}</label>
                            <div class="col-sm-9">
                                <select class="form-control" name="payment_svea_invoice_status">
                                    {% if payment_svea_invoice_status %}
                                        <option value="1" selected="selected">{{ text_enabled }}</option>
                                        <option value="0">{{ text_disabled }}</option>
                                    {% else %}
                                        <option value="1">{{ text_enabled }}</option>
                                        <option value="0" selected="selected">{{ text_disabled }}</option>
                                    {% endif %}
                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="payment_svea_invoice_sort_order" class="col-sm-2 control-label">{{ entry_sort_order }}</label>
                            <div class="col-sm-9">
                                <input class="form-control" type="text" name="payment_svea_invoice_sort_order" value="{{ payment_svea_invoice_sort_order }}" size="1" />
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="payment_svea_invoice_payment_description" class="col-sm-2 control-label">{{ entry_payment_description }}</label>
                            <div class="col-sm-9">
                                <textarea class="form-control" rows="2" cols="30" name="payment_svea_invoice_payment_description">{{ payment_svea_invoice_payment_description }}</textarea>
                            </div>
                        </div>
                        <!--shipping billing-->
                        <div class="form-group">
                            <label for="payment_svea_invoice_shipping_billing" class="col-sm-2 control-label">
                                <span data-toggle="tooltip" title="{{ entry_shipping_billing_text }}">{{ entry_shipping_billing }}</span>
                            </label>
                            <div class="col-sm-9" style="padding-top:15px;">
                                {% if payment_svea_invoice_shipping_billing  ==  "0" %}
                                    <input type="radio" name="payment_svea_invoice_shipping_billing" value="1" />
                                    {{ entry_yes }}
                                    <input type="radio" name="payment_svea_invoice_shipping_billing" value="0" checked="checked" />
                                    {{ entry_no }}
                                {% else %}
                                    <input type="radio" name="payment_svea_invoice_shipping_billing" value="1" checked="checked" />
                                    {{ entry_yes }}
                                    <input type="radio" name="payment_svea_invoice_shipping_billing" value="0" />
                                    {{ entry_no }}
                                {% endif %}
                            </div>
                        </div>
                        <!-- autodeliver -->
                        <div class="form-group">
                            <label for="payment_svea_invoice_auto_deliver" class="col-sm-2 control-label">
                                <span data-toggle="tooltip" title="{{ entry_auto_deliver_text }}">{{ entry_auto_deliver }}</span>
                            </label>
                            <div class="col-sm-9">
                                <select class="form-control" name="payment_svea_invoice_auto_deliver">
                                    <option value="0" {% if payment_svea_invoice_auto_deliver  ==  '0'%} {{ 'selected="selected"' }}{% endif %} >{{ text_disabled }}</option>
                                    <option value="1" {% if payment_svea_invoice_auto_deliver  ==  '1'%} {{ 'selected="selected"' }}{% endif %} >{{ text_enabled }}</option>
                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="entry_distribution_type" class="col-sm-2 control-label">
                                {{ entry_distribution_type }}
                            </label>
                            <div class="col-sm-9">
                                <select class="form-control" name="payment_svea_invoice_distribution_type">
                                    <option value="Post" {% if payment_svea_invoice_distribution_type  ==  'Post'%} {{ 'selected="selected"' }}{% endif %} >{{ entry_post }}</option>
                                    <option value="Email" {% if payment_svea_invoice_distribution_type  ==  'Email'%} {{ 'selected="selected"' }}{% endif %} >{{ entry_email }}</option>
                                </select>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="payment_svea_invoice_product_price" class="col-sm-2 control-label">
                                <span data-toggle="tooltip" title="{{ entry_product_text }}">{{ entry_product }}</span>
                            </label>
                            <div class="col-sm-9" style="padding-top:15px;">
                                    {% if payment_svea_invoice_product_price %}
                                        <input type="radio" name="payment_svea_invoice_product_price" value="1" checked="checked" />
                                        {{ entry_yes }}
                                        <input type="radio" name="payment_svea_invoice_product_price" value="0" />
                                        {{ entry_no }}
                                    {% else %}
                                        <input type="radio" name="payment_svea_invoice_product_price" value="1" />
                                        {{ entry_yes }}
                                        <input type="radio" name="payment_svea_invoice_product_price" value="0" checked="checked" />
                                        {{ entry_no }}
                                    {% endif %}
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="payment_svea_invoice_peppol" class="col-sm-2 control-label">
                                {{ entry_show_peppol_field }}
                            </label>
                            <div class="col-sm-9" style="padding-top:15px;">
                                {% if payment_svea_invoice_peppol %}
                                    <input type="radio" name="payment_svea_invoice_peppol" value="1" checked="checked" />
                                    {{ entry_yes }}
                                    <input type="radio" name="payment_svea_invoice_peppol" value="0" />
                                    {{ entry_no }}
                                {% else %}
                                    <input type="radio" name="payment_svea_invoice_peppol" value="1" />
                                    {{ entry_yes }}
                                    <input type="radio" name="payment_svea_invoice_peppol" value="0" checked="checked" />
                                    {{ entry_no }}
                                {% endif %}
                            </div>
                        </div>
                        <!-- Countrycode specific -->
                        <div class="tab-content" id="tab-invoice" >
                            <ul class="nav nav-tabs" id="payment_svea_country">
                                {% for code in credentials %}
                                    <li><a href="#tab-{{ code.lang}}" data-toggle="tab">{{ code.lang }}</a></li>
                                {% endfor %}
                            </ul>
                            <div class="tab-content">
                                {% for code in credentials %}
                                    <div class="tab-pane" id="tab-{{ code.lang }}">
                                        <div class="form-group">
                                            <label class="col-sm-2 control-label" for="{{ code.name_testmode }}">{{ entry_testmode }}</label>
                                            <div class="col-sm-9">
                                                <select class="form-control" name="{{ code.name_testmode }}">
                                                    <option value="1" {% if code.value_testmode  ==  '1'%} {{ 'selected="selected"' }}{% endif %} >{{ text_enabled }}</option>
                                                    <option value="0" {% if code.value_testmode  ==  '0'%} {{ 'selected="selected"' }}{% endif %} >{{ text_disabled }}</option>
                                                </select>
                                            </div>
                                        </div>
                                        <div class="form-group">
                                            <label class="col-sm-2 control-label" for="{{ code.name_username }}">{{ entry_username }}</label>
                                            <div class="col-sm-9">
                                                <input class="form-control" name="{{ code.name_username }}" type="text"
                                                       value="{{ code.value_username }}" />
                                            </div>
                                        </div>
                                        <div class="form-group">
                                            <label class="col-sm-2 control-label" for="{{ code.name_password }}">{{ entry_password }}</label>
                                            <div class="col-sm-9">
                                                <input class="form-control" name="{{ code.name_password }}" type="text"
                                                       value="{{ code.value_password }}" />
                                            </div>
                                        </div>
                                        <div class="form-group">
                                            <label class="col-sm-2 control-label" for="{{ code.name_clientno }}">{{ entry_clientno }}</label>
                                            <div class="col-sm-9">
                                                <input class="form-control" name="{{ code.name_clientno }}" type="text"
                                                       value="{{ code.value_clientno }}" />
                                            </div>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        </div>
                    </form>
                </div>
            </div><!-- panel-default -->
        </div><!-- container-fluid -->

        {% endif %}
<div style="height:100px"></div>
  <script type="text/javascript"><!--
$('#payment_svea_country a:first').tab('show');
//--></script>
{{ footer }}